# Опциональный внешний порт RabbitMQ для контроллеров автомоек
# Раскомментируйте при необходимости подключения оборудования

services:
  rabbitmq:
    ports:
      - "${RABBITMQ_EXTERNAL_PORT:-5672}:5672"
