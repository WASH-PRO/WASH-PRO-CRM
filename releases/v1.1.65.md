v1.1.65 — Faster Telegram bot start + egress probe · Быстрый старт бота и проверка Telegram

---

## English

### Fixed

- **Slow bot Start** — `deleteWebhook` no longer calls Telegram directly from Docker (hung up to 15s); uses host egress with a 5s cap.
- **telegram-egress** — fail-fast connect timeout + `GET /health/telegram` probe; `fix-pyorch` prints host→Telegram reachability.

### Upgrade

```bash
# Settings → Software updates → CRM → v1.1.65
# Then Stop → Start the informational bot
```

---

## Русский

### Исправлено

- **Долгий «Запуск» бота** — `deleteWebhook` шёл в Telegram напрямую из Docker и висел; теперь через egress, таймаут 5с.
- **telegram-egress** — быстрый connect-timeout и `GET /health/telegram`; в repair видна доступность Telegram с хоста.

### Обновление

```bash
# Настройки → Обновления ПО → CRM → v1.1.65
# Затем Стоп → Запуск информационного бота
```
