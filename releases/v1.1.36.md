v1.1.36 — CRM update no longer fails on modules-bridge · Обновление не падает из‑за modules-bridge

---

## English

### Fixed

- **CRM auto-update failed (v1.1.34/35)** — health no longer fails the whole update when `modules-bridge` is down; **dashboard always rebuilds**
- **WASH_HOST_PROJECT_ROOT** — auto-detected and saved to `.env` on pull step
- **Build/seed** — `modules-bridge` is best-effort; core CRM update can complete

### Steps on 192.168.1.151

1. **Settings → Software updates** → update to **v1.1.36** (should show **completed**, not failed)
2. Hard refresh browser (Cmd+Shift+R)
3. **Settings → Integrity and repair → Check → Rebuild modules-bridge + dashboard → Apply**
4. Open `/modules` again

---

## Русский

### Исправлено

- **Автообновление падало (v1.1.34/35)** — health больше не валит обновление без modules-bridge; **dashboard всегда пересобирается**
- **WASH_HOST_PROJECT_ROOT** — автоопределение и запись в `.env` при pull
- **Сборка/seed** — modules-bridge best-effort; ядро CRM обновляется

### Шаги на 192.168.1.151

1. **Настройки → Обновления ПО** → **v1.1.36** (должно быть **completed**)
2. Жёсткое обновление браузера (Cmd+Shift+R)
3. **Настройки → Диагностика и исправления → Проверить → Пересобрать modules-bridge + dashboard → Применить**
4. Открыть `/modules`
