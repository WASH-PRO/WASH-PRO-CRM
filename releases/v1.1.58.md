v1.1.58 — Vendored PyOrchestrator main.py import fix · Импорт create_script/storage_service в PyOrch

---

## English

### Fixed

- **PyOrchestrator `backend/app/main.py`** — add missing `create_script` / `storage_service` imports used at startup and demo seed (was only applied via `local/apply-server-patches.sh` on some hosts). Fresh installs from GitHub match the working `192.168.1.151` tree without a server-side patch.

### Upgrade

```bash
# Dashboard → Settings → Software updates → CRM
# or:
git fetch origin && git checkout v1.1.58
docker compose up -d --build pyorch-backend
```

After upgrade you can remove the old sed block from `local/apply-server-patches.sh` if present.

---

## Русский

### Исправлено

- **PyOrchestrator `backend/app/main.py`** — добавлены недостающие импорты `create_script` / `storage_service` (использовались при старте и seed; раньше только через `local/apply-server-patches.sh`). Чистая установка с GitHub совпадает с рабочей копией без серверного патча.

### Обновление

```bash
# Настройки → Обновления ПО → CRM
# или:
git fetch origin && git checkout v1.1.58
docker compose up -d --build pyorch-backend
```

После обновления можно убрать старый sed из `local/apply-server-patches.sh`, если он ещё есть.
