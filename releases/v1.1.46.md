v1.1.46 — Settings layout polish · Косметика: MQTT на всю ширину

---

## English

### Changed

- **Settings → MQTT (CRM)** — section spans full page width; login, password, and retention fields in a 3-column grid; redelivery options in a 4-column row on wide screens

### Upgrade

```bash
git pull
docker compose build dashboard
docker compose up -d dashboard
```

---

## Русский

### Изменено

- **Настройки → MQTT (CRM)** — блок на всю ширину; логин, пароль и retention в сетке из 3 колонок; параметры повторной доставки — в 4 колонки на широких экранах

### Обновление

```bash
git pull
docker compose build dashboard
docker compose up -d dashboard
```
