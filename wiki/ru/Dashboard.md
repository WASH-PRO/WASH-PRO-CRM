> **[English](en-Dashboard)** · **Русский** · [← Wiki](Home)

# Dashboard

Полное описание: [docs/dashboard.md](https://wash-pro.github.io/WASH-PRO-CRM/ru/dashboard/)

## Меню (v1.1.14)

| Группа | Разделы |
|--------|---------|
| **Главное** | Обзор, **Состояние** |
| **Объекты** | Автомойки, Посты |
| **Данные** | MQTT |
| **Карты** | Скидочные, Сервисные, VIP, Инкассация |
| **Аналитика** | Использование, Финансы, Архив |
| **Справочники** | Режимы работы, Валюты, Типы скидок *(Admin)* |
| **Автоматизация** | **Публикации**, **Telegram**, **MCP сервер**, Бэкапы *(Admin)* |
| **Система** | **Информация**, Уведомления, Пользователи, Группы, Настройки, Логи |

Admin-разделы — только **Administrator**. Профиль — в шапке (`/profile`). Мастер настройки — `/setup`.

## Язык интерфейса (v1.1.13+)

- **English** (по умолчанию) и **Русский** — меню, статусы, сообщения, логи UI
- Переключатель в **шапке** (флаги 🇺🇸/🇷🇺; одна иконка) и в **Настройках**
- Выбор сохраняется в браузере (`wash_locale`); пользовательские данные не переводятся

## Уведомления (v1.1.14)

В списке **Уведомления** и на Обзоре показываются **локализованные шаблоны по типу события** (`mqtt_credit`, `user_login`, `wash_created` и др.), а не сырой текст из БД. Параметры (сумма, логин, имя объекта) извлекаются из старых записей. Метки severity и CSV-экспорт следуют выбранному языку.

## Статус поста онлайн/оффлайн

На страницах **Посты**, **Состояние** и **карточка поста**.

- **Онлайн** — телеметрия за последние 30 с (`lastMessageAt`)
- **Офлайн** — нет данных или устарели

## Раздел «Публикации»

Новости и акции для информационного Telegram-бота (**Автоматизация → Публикации**, `/info-messages`). Статус **«По расписанию»** с прошедшей датой публикации отображается как **«Опубликовано»** (v1.1.12).

Массовое создание тестовых записей:

```bash
node scripts/seed-info-messages.mjs
# COUNT=50 START_DELAY_MIN=5 INTERVAL_MIN=10
```

## MCP сервер (`/mcp`)

- Dynamic API и PyOrchestrator (если включён)
- HTTP URL и конфиг для Cursor
- Таблица зарегистрированных инструментов

См. [MCP](ru-MCP).

## Настройки устройства поста

На странице поста (`/posts/:id#device-settings`) или через **⚙** в списке постов:

- **Цены режимов** — work-modes 0–9, MQTT `set/prices`
- **Команды** — перезагрузки, зачисление, сервисные режимы → `set/command`
- **Префикс MQTT** (`dt_pref`, обычно `washpro`)
- **Логин/пароль MQTT** — в форме создания/редактирования поста

## Resources (сайдбар)

- **Dynamic API** → `:8080` + статус
- **PyOrchestrator** → `:8090` + статус
- Документация, GitHub

## Live-обновление

| Интервал | Страницы |
|----------|----------|
| 3 с | Состояние, Посты, карточка поста |
| 5 с | Обзор |
| 30 с | **Информация** (`/system`, состояние платформы) |
| 10–15 с | Остальные |

## Недавние изменения (v1.1.28)

- **Справка** — пункт внизу sidebar (над «Документация»), без выделения; убрана из шапки

## Недавние изменения (v1.1.27)

- **Справка** — кнопка в шапке и пункт в боковом меню
- **Кэш JS** — nginx 404 для устаревших `/assets/`; понятное сообщение при ошибке загрузки

## Недавние изменения (v1.1.26)

- **Версия CRM** — `APP_VERSION` до сборки; `version.json` при старте контейнера

## Недавние изменения (v1.1.25)

- **Docker Hub** — понятная ошибка при таймауте; docs/troubleshooting для Mac
- **Справка и подсказки** — failed job и раздел «Настройки»

## Недавние изменения (v1.1.24)

- **Failed job** — устаревшая ошибка скрывается, если версия уже новее

## Недавние изменения (v1.1.23)

- **Автообновление** — `APP_VERSION` в `.env` только после успешного job; откат при ошибке
- **Версия CRM** — из `/version.json` работающего dashboard
- **Failed job** — кнопки «Повторить» и «Скрыть ошибку»

## Недавние изменения (v1.1.22)

- **Встроенная справка** — полноэкранное окно из шапки (иконка книги), 26 разделов, схемы экранов, EN/RU
- **Меню** — «Система» (`/system`) перенесена в группу **Система** и переименована в **Информация**; раздел бота — **Публикации**
- **Хлебные крошки** — цепочка «группа → раздел» как в меню; исправлены ссылки
- **i18n** — короткая подпись «Использование» в свёрнутом меню (RU)

## Недавние изменения (v1.1.21)

- **Целостность** — ложное «/deploy не git-репозиторий» устранено (`git safe.directory`)
- **Карточка обновлений** — старый failed job скрывается после успешного обновления

## Недавние изменения (v1.1.20)

- **Автообновление CRM** — `git reset --hard origin/main`; не блокируется случайными правками отслеживаемых файлов
- **Ошибка на карточке** — failed job с логом шага, не исчезает сразу
- **Compose override** — build/seed через `scripts/compose-files.sh`

## Недавние изменения (v1.1.19)

- **Целостность** — внешние пути `DATA_DIR` (`/mnt/hdd/data`, `/var/lib/wash-pro-crm`) больше не помечаются как подозрительные; предупреждение только при `DATA_DIR` внутри `/deploy`

## Недавние изменения (v1.1.18)

- **Обновления ПО** — проверка релизов через `git ls-remote` без `GITHUB_TOKEN`; загрузка страницы не сбрасывает уведомление
- **«Проверить сейчас»** — единственный принудительный опрос GitHub; опрос прогресса job — из кэша
- **Примеры** — `docker-compose.override.yml.example`, `local/apply-server-patches.sh.example` для серверных правок

## Недавние изменения (v1.1.17)

- **Настройки → Целостность и исправление** — диагностика `WASH_HOST_PROJECT_ROOT`, `DATA_DIR`, ключевых файлов; исправления (Mosquitto, `init-seed`, зависшие обновления)
- **`update-bridge`** — `GET/POST /api/crm/updates/repair`

## Недавние изменения (v1.1.16)

- **Информация** (`/system`) в группе **«Система»** — ресурсы сервера, версия WASH PRO CRM, стек Dynamic API / PyOrchestrator
- **Модель CPU** — корректное отображение в Docker (через `/proc/cpuinfo`)

## Недавние изменения (v1.1.15)

- **Обзор** — график **загруженности по дням** под графиком выручки
- **Уведомления** — кнопка «Удалить все»
- **Telegram** — надёжная остановка, `stop-all`, демо-боты при установке; информационный бот v2.2 (занятость: свободен только `program_9`)

## Ранее (v1.1.14)

- **Локализация уведомлений** — заготовленные фразы по типу; старые записи на выбранном языке
- **Шапка** — язык и Live/Static одной иконкой на всех экранах

## Ранее (v1.1.13)

- v1.1.12: группа **Автоматизация**, MCP, стабильность навигации
- Пагинация таблиц: 20/40/60/80/100
- Мастер настройки, онлайн/оффлайн постов
- Уведомления web + Telegram
