# WASH-PHO-CRM local patches for vendored dynamic-api/
# Re-apply after running scripts/update-dynamic-api.sh
#
# 1. backend/src/app.ts — CORS: несколько origin через запятую (docker-compose CORS_ORIGIN)
# 2. frontend/src/services/api.ts — понятная ошибка при 502 / не-JSON ответе nginx
# 3. frontend/src/pages/UsersPage.tsx — Safari: type=text + noValidate для email
# 4. patches/wash-embedded/ — WASH UI в панели (VITE_WASH_EMBEDDED): Settings, UpdateBanner
#
# resourcePath / DELETE по :id — в upstream (getCollectionPath), отдельный патч не нужен.
#
# WASH-специфика docker-compose:
# - build context: ./dynamic-api (не backend/) — нужны scripts/ для v1.5+
# - UPDATE_EXECUTOR_ENABLED=false — in-app updater не применим к vendored-копии
