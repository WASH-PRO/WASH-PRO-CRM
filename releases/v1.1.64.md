v1.1.64 — Telegram egress deploy + dashboard rolldown fix · Деплой egress и сборка dashboard

---

## English

### Fixed

- **v1.1.63 deploy** — CRM update failed on dashboard Vite/rolldown native binding; Dockerfile installs `@rolldown/binding-linux-x64-gnu`. Pyorch/`telegram-egress` builds first so bots get egress even if dashboard rebuild is slow.
- **fix-pyorch** — `docker compose up --build` for `telegram-egress` + bridge.

### Upgrade

```bash
# Dashboard → Settings → Software updates → CRM → v1.1.64
# Then stop/start the informational Telegram bot
```

---

## Русский

### Исправлено

- **Деплой v1.1.63** — падала сборка dashboard (rolldown binding); pyorch/`telegram-egress` собираются первыми.
- **fix-pyorch** — `--build` для egress и bridge.

### Обновление

```bash
# Настройки → Обновления ПО → CRM → v1.1.64
# Затем Стоп → Запуск информационного бота
```
