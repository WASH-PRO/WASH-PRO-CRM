v1.1.52 — Notifications like MQTT · Уведомления как MQTT

---

## English

### Fixed

- **Notifications page** — same server pagination as MQTT: **100 rows**, single **Load more** button above the table, subtitle with shown/total/updated, **3 s** polling

### Documentation

- **GitHub Pages, wiki, release notes** — updated for v1.1.48–v1.1.52 (post history, archive UX, update-bridge, MongoDB indexes, troubleshooting); PyOrchestrator **v0.1.13**; fixed broken doc links

### Upgrade

Auto-update from Dashboard (**Settings → Updates**) or:

```bash
git pull
docker compose build dashboard
docker compose up -d dashboard
```

---

## Русский

### Исправлено

- **Страница уведомлений** — серверная пагинация как в MQTT: **100 записей**, одна кнопка **«Загрузить ещё»** над таблицей, подзаголовок с количеством и временем обновления, опрос **3 с**

### Документация

- **GitHub Pages, wiki, release notes** — актуализированы v1.1.48–v1.1.52 (история поста, архив, update-bridge, индексы MongoDB, troubleshooting); PyOrchestrator **v0.1.13**; исправлены битые ссылки

### Обновление

Автообновление из Dashboard (**Настройки → Обновления**) или:

```bash
git pull
docker compose build dashboard
docker compose up -d dashboard
```
