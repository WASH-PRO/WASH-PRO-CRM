v1.1.38 — Modules page sections + docs version sync · Разделы на странице «Модули» + синхронизация версии

---

## English

### Changed

- **Modules page** — separate **Installed** and **Available** sections; per-section pagination; removed redundant “Installation” filter
- **Docs & wiki** — version **v1.1.38**; `app-version.sh` / `sync-version-docs.sh`; GitHub Pages badge synced from `dashboard/package.json` in CI

### Upgrade

1. **Settings → Software updates** → update to **v1.1.38**
2. Open **Automation → Modules** — installed and catalog modules are in separate blocks

---

## Русский

### Изменено

- **Страница «Модули»** — разделы **Установленные** и **Доступные**; пагинация в каждом блоке; убран лишний фильтр «Установка»
- **Документация и wiki** — версия **v1.1.38**; скрипты `app-version.sh` / `sync-version-docs.sh`; бейдж GitHub Pages синхронизируется из `dashboard/package.json` в CI

### Обновление

1. **Настройки → Обновления ПО** → обновление до **v1.1.38**
2. **Автоматизация → Модули** — установленные и каталог в отдельных блоках
