# Устарело: порты MQTT теперь в docker-compose.yml по умолчанию.
# Оставлено для обратной совместимости со старыми инструкциями.

services:
  mosquitto:
    ports:
      - "${MQTT_PORT_PUBLISH:-1883:1883}"
