v1.1.4 — Page size selector · Селектор размера страницы

**English** · **Русский**

---

## 🇬🇧 English

### Changed

- **Table pagination** — per-page selector **50 / 100 / 200 / 500 / 1000** (default **100**) and **Load more (N rows)** button (MQTT-style)
- Shared `DataTable` component — applies to all CRM tables

### Upgrade

```bash
git pull
docker compose up -d --build --no-deps dashboard
```

---

## 🇷🇺 Русский

### Изменено

- **Пагинация таблиц** — выпадающий список **50 / 100 / 200 / 500 / 1000** (по умолчанию **100**) и кнопка **«Загрузить ещё»**
- Общий компонент `DataTable` — все таблицы CRM

### Обновление

```bash
git pull
docker compose up -d --build --no-deps dashboard
```

---

Full changelog · Полный список: [CHANGELOG.md](https://github.com/WASH-PRO/WASH-PRO-CRM/blob/main/CHANGELOG.md)
